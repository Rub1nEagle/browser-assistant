# Pinned versions of the direct runtime dependencies.
# Reproduce the environment with: pip install -r requirements.txt
#
# pyproject.toml keeps loose ranges (the library declaration); this file
# is the concrete environment lock used for builds and reviews.

playwright==1.59.0
anthropic==0.97.0
openai==1.109.1
rich==15.0.0
click==8.3.3
python-dotenv==1.2.2
pydantic==2.13.3
fastapi==0.136.1
uvicorn[standard]==0.46.0
websockets==16.0
